## Why
列举完成后,`#dlTarget`(目标目录预览)虽已被 JS 正确填充(`下载到: F:\...`),但用户看不到。
根因:该 `<span>` 是"开始下载"按钮所在 flex 行的兄弟节点,设了 `max-width:100%`
(相对整个被按钮占用的 flex 容器)。一旦路径较长,文本在不可换行的 flex 行里被挤到
右边缘之外被裁剪,视觉上"不出现"。

## What Changes
将 `#dlTarget` 从按钮所在的 flex 行中移出,改为按钮**下方独立的一整行(block、整宽、
可换行)**。这样无论路径多长都会显示在按钮下方,稳定可见。
